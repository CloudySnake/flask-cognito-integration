{
    "name": "template",
    "version": "1.0.0",
    "dependencies": {
        "serverless": "^1.65.0",
        "serverless-prune-plugin": "^1.4.1",
        "serverless-python-requirements": "^4.3.0",
        "serverless-wsgi": "^1.7.4"
    },
    "scripts": {
        "install-env": "pipenv install --dev",
        "test": "pipenv run test",
        "deploy-sandbox": "serverless deploy --stage sandbox"
    }
}
